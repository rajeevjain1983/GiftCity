import { HOME_ACTION_PATTERN } from '../../../../constants/reducer.constants';
const HOME_CONSTANTS = {
    GET_NAVIGATION: `${HOME_ACTION_PATTERN}GET_NAVIGATION`,
    SET_NAVIGATION: `${HOME_ACTION_PATTERN}SET_NAVIGATION`,
};



export default HOME_CONSTANTS;
