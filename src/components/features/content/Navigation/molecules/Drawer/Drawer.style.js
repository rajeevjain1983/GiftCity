// 9fbef606107a605d69c0edbcd8029e5d 
import { css } from 'styled-components';

export default css`
background:gray;
border:5px solid blue;


/* z-index: 1203; */

  /* @-webkit-keyframes slideIn {
    0% {
      transform: translateX(-900px);
    }
    100% {
      transform: translateX(0);
    }
  }
  @-moz-keyframes slideIn {
    0% {
      transform: translateX(-900px);
    }
    100% {
      transform: translateX(0);
    }
  }
  @keyframes slideIn {
    0% {
      transform: translateX(-900px);
    }
    100% {
      transform: translateX(0);
    }
  } */
  /* .tcp-drawer-overlay {
    background: ${props => props.theme.colors.common.black};
    position: absolute;
    z-index: 900;
    height: 100%;
    opacity: 0.6;
    width: 100%;
  }
  .is-hidden {
    display: none;
  }
  .tcp-drawer__isOpen_${props => props.navigationLevel} {
    display: block;
    position: absolute;
    z-index: 1200;
    top: ${props => (props.position && props.position.top) || 0};
    left: ${props => (props.position && props.position.left) || 0};
    width: 100%;
    height: ${props => props.height || `auto`};
  }
  .tcp-condensed-drawer {
    position: fixed;
    top: 58px;
  }
  .tcp-drawer-content {
    width: ${props => props.width.small || `100%`};
    background: green;
    height: ${props => props.height || `auto`};
  }
  .display-small-none {
    display: none;
  }
  
  .manage-account {
    position: absolute;
    top: 35%;
    right: 6%;
  }
  .rightLink {
    border-left: 1px solid ${props => props.theme.colors.gray[500]};
    box-sizing: border-box;
    margin-left: ${props => props.theme.spacing.ELEM_SPACING.XS};
    padding-left: ${props => props.theme.spacing.ELEM_SPACING.XS};
    min-width: ${props => props.theme.spacing.ELEM_SPACING.XXXL};
    border-radius: 0px;
  }
  .input-checkbox-wrapper {
    display: flex;
  }
  @media ${props => props.theme.mediaQuery.medium} {
    .tcp-drawer__isOpen {
      top: ${props => (props.position && props.position.topMedium) || 0};
    }
    .tcp-condensed-drawer {
      position: fixed;
      top: 62px;
    }
    .tcp-drawer-content {
      width: ${props => props.width.medium || `100%`};
    }
    .display-small-none {
      display: block;
    }
    .display-medium-none {
      display: none;
    }
  }
  @media ${props => props.theme.mediaQuery.large} {
    .tcp-drawer-content {
      width: ${props => props.width.large || `100%`};
      background: none;
    }
    .display-medium-none {
      display: block;
    }
    .display-large-none {
      display: none;
    }
  } */
`;
